EventMesh Schema

Avoir une représentation graphique des échanges entre les applications via un "event designer".

Un "domain" possède un ensemble d'applications ainsi qu'un RootTopic.

Un événement qui peut être envoyé d'une application à une autre possède un JSON schema.

Pour chaque application on peut télécharger sa définition  ASYNCAPI.

Il faut pouvoir router les messages du topic vers la queue

Pour l'écran d'édition 

Il faut créer un "domain" dans lequel on peut ajouter les applications.
Chaque application possède son propre asyncapi
Un processus serverless workflow est finalement une application qui peut être éditer !!!


Nous avons un eventmesh

Un client va souscrire à un ou plusieurs topics

Un eventmesh server peut posséder un ou plusieurs VPN (serveur virtuel).
Chaque client est lié à un VPN
Il peut soucrire à un ou plusieurs topics
Chaque VPN possède une collection d'applications & événements.

Un VPN peut être lié à un autre VPN (VPN bridge).

Créer un domaine
Pour le domaine il faut ajouter une ou plusieurs applications.
Pour le workflow il faut sélectionner le VPN, choisir le domain, choisir l'application.

-------
Nous avons un EventMeshServer
Ce dernier peut posséder un ou plusieurs VPN
Chaque VPN peut posséder un ou plusieurs application domains
Chaque "application domain" peut posséder une ou plusieurs applications
Un client peut se connecter à un VPN, il possède sa propre définition. Il peut ainsi publier un message & ce dernier va être publié vers les autres applications
Comment lier tout cela à serverless workflow ?
Serverless workflow n'est simplement qu'une application qui agit dans son propre application domain
-------

1. Ajouter le concept de VPN

